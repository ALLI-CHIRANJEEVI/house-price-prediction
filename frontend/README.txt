Frontend Instructions:

- Open index.html in your browser.
- Upload a CSV file and click 'Upload & Process'.
- The file will be sent to the backend at http://localhost:5000/process.
- After processing, download the output CSV.

Backend must provide a /process endpoint that accepts file uploads and returns the processed CSV.
